import ref = require('ref-napi');

export const ENUM = ref.types.uint32;
export const BOOL = ref.types.uint32;
export const DWORD = ref.types.uint32;
export const SIZE_T = ref.types.size_t;

export const HANDLE = ref.types.size_t;
export const HWND = HANDLE;

export const HRESULT = ref.types.uint32;
